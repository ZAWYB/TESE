# 📖 Git 学习与实战笔记

这里记录了在实际使用 Git 过程中遇到的高频命令和特殊配置技巧。

---

## 1. 远程仓库管理

### 修改远程仓库地址 (切换至 SSH)
在使用 Git 的过程中，为了实现免密提交代码，通常需要将远程仓库的连接方式从传统的 HTTPS 更改为 SSH。

**执行命令：**
```bash
git remote set-url origin git@github.com:ZAWYB/PDF.git

**命令解析：**
* `remote`: 针对远程仓库的操作。
* `set-url`: 更改已存在的远程仓库地址。
* `origin`: 默认的远程仓库别名（通常你的云端仓库都叫这个名字）。
* `git@github.com...`: 你的全新 SSH 格式仓库地址。

# 📝 Git 学习与实战笔记

## 1. 终端基础命令
* **`ls`**
  * **作用**：列出当前文件夹下的所有文件。
  * **场景**：在执行 `git add` 之前，看一眼当前目录下有哪些文件，防止添加错。（注：`ls -a` 可以看到隐藏的 `.git` 文件夹）。

## 2. 仓库初始化与连接 (打地基)
* **`git init`**
  * **作用**：把当前普通文件夹变成一个 Git 可以管理的本地仓库。
* **`git branch -M main`**
  * **作用**：将默认的主分支名称重命名为 `main`。
* **`git remote add origin <仓库地址>`**
  * **作用**：给本地仓库绑定一个云端的“大本营”（如 GitHub 仓库）。
* **`git remote set-url origin <SSH仓库地址>`**
  * **作用**：修改已有的远程仓库地址（例如从 HTTPS 改成 SSH，实现免密操作）。
* **`git remote -v`**
  * **作用**：查看当前绑定的远程仓库地址到底是什么。

## 3. 日常提交流程 (核心动作)
* **`git status`**
  * **作用**：查看当前文件的状态（红字代表被修改但没追踪，绿字代表已在暂存区）。**建议每次操作前都敲一下。**
* **`git add <文件名>`** (例如：`git add README.md`)
  * **作用**：把文件放入暂存区。
* **`git commit -m "这里写备注"`**
  * **作用**：正式生成一条历史快照记录。
* **`git push`**
  * **作用**：把本地最新的 commit 历史推送到云端仓库保存。
* **`git push -u origin main`**
  * **作用**：首次推送时使用，建立本地与云端 `main` 分支的永久关联。
* **`git pull`**
  * **作用**：与 `push` 相反，把云端仓库最新的代码**拉取**并同步到本地。

## 4. 查看差异、历史与配置 
* **`git config --list`**
  * **作用**：列出当前 Git 的所有全局和本地配置。
* **`git diff`**
  * **作用**：查看文件具体被修改了哪几行（按 `q` 退出）。
* **`git log`**
  * **作用**：查看完整的历史提交记录。
* **`git log --oneline`**
  * **作用**：查看精简版的提交记录（一行一条）。
* **`git log -1`**
  * **作用**：只查看最近的 1 条提交记录（数字可改）。
* **`git restore <文件名>`**
  * **作用**：撤销未 commit 的修改，瞬间恢复到上一次干净的状态。

## 5. 分支管理与高阶操作 (进阶目标)
* **`git branch`**
  * **作用**：查看当前有哪些分支，或创建新分支。
* **`git switch <分支名>`**
  * **作用**：在不同的分支之间进行切换。
* **`git merge <分支名>`**
  * **作用**：把其他分支的代码合并到你当前的分支里。
* **`git reset`** 和 **`git revert`**
  * **作用**：版本回滚。用来撤销已经提交的错误记录（吃后悔药）。

---

## 💡 避坑小锦囊 (今日排错记录)

**1. 推送失败：`error: src refspec main does not match any`**
* **原因**：执行 `git add` 后忘记执行 `git commit`，导致本地没有任何实际的历史记录可以推送。
* **解决**：补上 `git commit -m "备注"` 后再次 `push` 即可。

**2. 添加远程仓库报错：`fatal: remote origin already exists.`**
* **原因**：本地已经绑定过一个叫 `origin` 的远程仓库了，不能重复 `add`。
* **解决**：如果需要改地址，应该使用修改命令 `git remote set-url origin <新地址>`。